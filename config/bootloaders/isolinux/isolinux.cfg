include menu.cfg
default vesamenu.c32
prompt 0
# Auto-boot the first entry (Live system) after 5 seconds (units = 1/10 s)
# so the kiosk starts on its own without a keypress. Set to 0 to wait forever.
timeout 50
